import { styled, Box } from "@mui/material";

export const MainContainer = styled(Box)({
  display: "flex",
  flexFlow: "column",
  background: "#29203c",
  borderRadius: "12px",
  boxSizing: "border-box",
  padding: "25px",
});
