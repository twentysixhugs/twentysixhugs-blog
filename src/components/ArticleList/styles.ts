import { styled } from "@mui/material";

export const MainContainer = styled("ul")({
  display: "flex",
  flexFlow: "column",
  gap: "32px",
  margin: 0,
  padding: 0,
  listStyle: "none",
});

export const Li = styled("li")({
  margin: 0,
  padding: 0,
  listStyle: "none",
});
